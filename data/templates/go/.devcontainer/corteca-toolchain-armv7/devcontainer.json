{
  "name": "corteca-toolchain-armv7",
  "image": "{{ .build.toolchains.image }}",
  "runArgs": [
    "--platform=linux/arm/v7"
  ],
  "mounts": [
    {
      "source": "${localWorkspaceFolder}",
      "target": "/app",
      "type": "bind"
    }
  ]
}