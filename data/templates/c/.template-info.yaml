name: "c"
description: "An application template for the C language."
dependencies:
  compile: [ gcc ]
  runtime: [ json-c ]
regenFiles:
  .corteca/ADF.template: ADF
  .corteca/Dockerfile.template: Dockerfile
options:
